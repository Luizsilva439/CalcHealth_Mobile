import React from "react";
import { View, Text } from "react-native";
import styles from "./style";

export default function Title() {
  return (
    <View style={styles.boxText}>
      <Text style={styles.styleText} >CALC-HEALTH</Text>
    </View>
  );
}